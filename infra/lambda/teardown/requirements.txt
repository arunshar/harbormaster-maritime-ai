# Local testing only.
#
# boto3 ships in the AWS Lambda Python runtime, so it is NOT bundled into the
# deployment package. This file exists so you can create a local virtualenv and
# run handler.py / test_handler.py off-cloud.
#
#   python -m venv .venv
#   . .venv/bin/activate
#   pip install -r requirements.txt
#
boto3>=1.34.0
