# Phase 5 gate 5.2: namespace for the EKS-hosted serving front door.
# Sibling to deploy/k8s/cdc's hm-cdc (the Phase 2 local plane); hm-serving is
# the EKS migration target for the ECS Fargate serving service.
apiVersion: v1
kind: Namespace
metadata:
  name: hm-serving
  labels:
    app.kubernetes.io/part-of: harbormaster
